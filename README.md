An application was developed in Flask and PostgreSQL with authentication support.
The /profile route is accessible only to an authorized user. The profile displays a welcome message with the name of the current user. Authentication occurs using flask session
The ability to log out of your profile is available, which is implemented in the /logout route